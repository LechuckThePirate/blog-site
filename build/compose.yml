version: "3.9"

services:
  api:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app

  mysql:
    image: mysql:latest
    environment:
      MYSQL_ROOT_PASSWORD: password
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
