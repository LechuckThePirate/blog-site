{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "ConnectionStrings": {
    "mysql": "Server={{DB_HOST}};Database={{DB_NAME}};Uid={{DB_USER}};Pwd={{DB_PASSWORD}};Port={{DB_PORT}}"
  }
}
